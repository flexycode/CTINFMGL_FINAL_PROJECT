<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Required meta tags for email client compatibility -->
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    
    <!-- Preconnect for Google Fonts to improve loading speed -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    
    <!-- Using Press Start 2P for a gaming-style font -->
    <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">
    
    <title>Email Confirmation</title>
    
    <!-- All styles must be inline for maximum email client compatibility -->
    <style>
        /* Base styles for consistent rendering across email clients */
        body {
            font-family: Arial, sans-serif;
            color: #333;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            /* Using tables for layout as recommended for email compatibility */
            display: table;
            width: 100%;
        }
        
        /* Container for the email content */
        .email-container {
            max-width: 600px;
            margin: 30px auto;
            padding: 20px;
            background-color: #ffffff;
            border: 2px solid #ddd;
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
            /* Using inline-block to ensure proper centering in older clients */
            display: inline-block;
        }
        
        /* Header styles */
        h1 {
            font-size: 24px;
            color: #4caf50;
            margin-bottom: 20px;
            /* Using fallback font for email clients that don't support Google Fonts */
            font-family: 'Press Start 2P', monospace;
        }
        
        /* Common styles for information sections */
        .details, .ticket-info, .purchase-summary {
            font-size: 16px;
            color: #555;
            text-align: left;
            margin: 0 auto;
            max-width: 320px;
        }
        
        /* Label styles for better readability */
        .details span.label,
        .ticket-info span.label,
        .purchase-summary span.label {
            font-weight: bold;
            color: #333;
            display: block;
        }
        
        /* Ticket information section with subtle background */
        .ticket-info {
            background-color: #f7f7f7;
            padding: 15px;
            border-radius: 8px;
            margin-top: 20px;
        }
        
        /* Purchase summary section with different background */
        .purchase-summary {
            background-color: #f0f8ff;
            padding: 15px;
            border-radius: 8px;
            margin-top: 20px;
        }
        
        /* Thank you message styling */
        .thank-you {
            font-size: 18px;
            color: #4caf50;
            font-weight: bold;
            margin-top: 20px;
        }
        
        /* Footer styling */
        .footer {
            font-size: 14px;
            color: #777;
            margin-top: 20px;
        }
        
        /* Ticket header styling */
        .ticket-header {
            font-size: 20px;
            color: #333;
            font-weight: bold;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <!-- Using center tag for maximum compatibility across email clients -->
    <center>
        <div class="email-container">
            <h1>Ticket Confirmation</h1>
            
            <!-- Passenger details section -->
            <div class="details">
                <p><span class="label">FlightId:</span><%= id %></p>
                <p><span class="label">Departure:</span><%= flightDeparture %></p>
                <p><span class="label">Arrival:</span><%= flightArrival %></p>
                <p><span class="label">Name:</span><%= name %></p>
                <p><span class="label">Email:</span><%= email %></p>
                <p><span class="label">Date:</span><%= date %></p>
                <p><span class="label">Price:</span><%= price %></p>
            </div>
            
            <!-- Ticket information section -->
            <div class="ticket-info">
                <div class="ticket-header">Ticket Information</div>
                <p><span class="label">Seat Number:</span><%= seatNumber %></p>
                <p><span class="label">Seat Class:</span><%= seatClass %></p>
            </div>
            
            <!-- Purchase summary section -->
            <div class="purchase-summary">
                <div class="ticket-header">Purchase Summary</div>
                <p><span class="label">Total Price:</span><%= price %>USD</p>
                <p><span class="label">Transaction ID:</span>123abc123b</p>
            </div>

            <!-- Thank you message -->
            <div class="thank-you">Thank you for your trust!</div>
            <div class="footer">Have a nice flight :)</div>
        </div>
    </center>
</body>
</html>
